syntax = "proto3";

option java_package = "com.example.services.storage";
option java_multiple_files = true;

message WhitePhoneNumber {
  string number = 1;
  ///should the number be sent to server
  bool is_shared = 2;
}
message BlackPhoneNumber {
  string number = 1;
}
